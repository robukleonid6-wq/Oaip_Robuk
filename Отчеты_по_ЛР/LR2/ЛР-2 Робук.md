### Задание 1
```mermaid
---

config:

      theme: redux

---

flowchart TD

        A(["Start"])

        A --> B[/"Пользователь вводит ip адрес или домен"/]

        B --> C{"Это доменное имя?"}

        C --Да--> D["Запрос к dns серверу"]

        C --Нет---> E["Получен ip адрес"]

        D --> E --> F["отправка icmp пакета"] --> G["Ожидание ответа"]

        G --> H{"Ответ получен?"}

        H --Нет--> J[/"Превышен интервал ожидания"/] --> K

        H --Да--> I["отправка время отклика"] --> K

        K{"Отправлено четыре пакета?"} --Нет--> F

        K --Да--> L[/"Вывод статистики запроса"/] ----> М(["End"])
```
### Задание 2
```mermaid
---

config:

  theme: redux

---

flowchart TD


            F --> H["смотрит на соседей и записывает стоимость до них"]

    H --> I["выбирает соседа с наименьшией стоимостью"]

    I --> J["через него смотрит следущих соседей"]

    J --> K{"новый путь короче?"}

    K -- нет --> J

    K -- да --> M["обновляет таблицу путей"]

    A(["Start"]) --> B[/"Пользователь вводит все команды"/]

    B --> C["устройство отправляет пакет"]

    C --> D{"есть путь в таблице?"}

    D -- нет ---> F["своя стоимость 0"]

    D -- да ---> G["отправляет пакет след. маршрутизатору"]

    G --> O["след. маршрутизатор получает пакет"] --> P

    P{"это конечная сеть?"} --нет--> D

    P --да--> Q["отправляет пакет получателю"] --> R[/"выводит интвормацию пользователю"/]
    R --> W(["end"])
```

### Задание 3

```mermaid
---

config:

  theme: redux

---

flowchart LR

    A([старт]) --> B[запрос подключения к серверу]

    B --> C[он отправляет свой ключ]

    C --> D[клиент проверяет ключ]

    D --> E{ключ верный?}

    E -- нет --> F[предупреждение о незнакомом ключе] --> G[/спросит пользователя: продолжить?/]

    G --> H{пользователь одобрил?}

    H -- нет --> I[подключение отклонено] --> Z([end])

    H -- да --> J

    E -- да --> J[запрос логина и пароля]

    J --> K[у\становка счётчика: попытки = 3]

    K --> L[/ввод логина и пароля/]

    L --> M[отправка данных на сервер]

    M --> N{пароль верный?}

    N -- да --> O[пароль верный] --> P[начало сеанса SSH] --> Q([end])

    N -- нет --> R[уменьшить счётчик попыток на 1]

    R --> S{попытки > 0?}

    S -- нет --> T[Сервер разрывает соединение] --> U([end])

    S -- да --> V[/вывод: неверный пароль/] --> L
```

### Задание 4

```mermaid
---

config:

  theme: redux

---

flowchart TD

    A([Начало]) --> B[/Ввод массива arr/]

    B --> C[min = arr'0']

    C --> D[i = 1]

    D --> E{i < len'arr'?}

    E -->|Да| F{arr'i' < min?}

    F -->|Да| G[min = arr'i']

    G --> H[i += 1]

    F -->|Нет| H

    H --> E

    E -->|Нет| I[Вывод min]

    I --> J[end]
```